# Data Folder

Use this folder only when running the API from a local Excel file instead of Google Sheets.

Expected local file:

```text
data/trading.xlsx
```

Local `.xlsx` and `.csv` files in this folder are ignored by Git to prevent private trading files from being committed.
